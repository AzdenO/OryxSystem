/**
 * @module Electron_Init
 * @description the main point of initial construction of electron front-end, defining functions for creating
 * windows, retrieving appropriate dependencies, etc.
 * @version 0.1
 * @author DRAW
 */
////////////////////////////////////////////////////////////////////////////////////////////////////////////
import {ElectronInit} from './Electron_Init.mjs';
import CommsHandler from "../root/CommsHandler.mjs";
////////////////////////////////////////////////////////////////////////////////////////////////////////////
function createElectron(dev, __dirname){

    const res = ElectronInit.createMainWindow(dev, __dirname);
}
////////////////////////////////////////////////////////////////////////////////////////////////////////////
function initElectron(){

    return ElectronInit.getMainDependencies();
}
////////////////////////////////////////////////////////////////////////////////////////////////////////////
function initCommsHandler(ipc){
    const comms = new CommsHandler(ipc);
    return comms;
}
////////////////////////////////////////////////////////////////////////////////////////////////////////////
export const MainInit = {
    initElectron,
    createElectron,
    initCommsHandler,
}