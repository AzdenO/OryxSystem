import Config from "./FileSystemConfig.json" with {type: "json"};
import AccessRegister from "./Registry/AccessRegister.mjs";
///////////////////////////////////////////////////////////////////////////////

export default class FileSystem{

    //////////////////////////////////////////////////////////////////////////
    constructor(args){
        this.library = args[0];
        this.console = args[1];
        this.sysSec = args[2];
        this.register = new AccessRegister();
    }
    //////////////////////////////////////////////////////////////////////////
    init(){
        this.fs = this.library.request(["fs"]);
    }
    //////////////////////////////////////////////////////////////////////////
    updatePartitions(){

    }
    //////////////////////////////////////////////////////////////////////////
    registerAccess(service){

    }
}