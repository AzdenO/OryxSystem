/**
 * @class Tool
 * @abstract
 * @description Base abstract class that all tools/services inherit from. All potential tools MUST inherit from
 * this class, or will not be loaded at runtime. Defines a common interface for all potential tools with
 * default tool lifecycle methods. All tools must export a single default class
 * classes must overwrite init() and commandprocessor()
 * @version 0.1
 * @author DRAW
 */
////////////////////////////////////////////////////////////////////////////////////////////////////
export default class Tool{

    ////////////////////////////////////////////////////////////////////////////////////////////////
    constructor(Library){
        this.library = Library;
    }
    ////////////////////////////////////////////////////////////////////////////////////////////////
    init(){

    }
    ////////////////////////////////////////////////////////////////////////////////////////////////
    handle(){

    }
    ////////////////////////////////////////////////////////////////////////////////////////////////
}